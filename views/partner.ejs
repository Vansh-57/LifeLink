<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Partner With Us - LifeLink Blood Donation</title>
    <style>
        /* ===== RESET & BASE STYLES ===== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }
        
        body {
            color: #333;
            overflow-x: hidden;
        }

        /* ===== ANIMATED BACKGROUND ===== */
        .background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background: linear-gradient(135deg, #f8f8f8 0%, #ffecec 100%);
            overflow: hidden;
        }

        .blood-cell {
            position: absolute;
            border-radius: 50%;
            background: rgba(200, 0, 0, 0.1);
            animation: float 15s infinite linear;
            filter: blur(0.8px);
        }

        .blood-cell:nth-child(1) {
            width: 150px;
            height: 150px;
            top: 10%;
            left: 10%;
            background: radial-gradient(circle, rgba(255,0,0,0.1) 0%, rgba(200,0,0,0.05) 70%);
        }

        .blood-cell:nth-child(2) {
            width: 200px;
            height: 200px;
            top: 60%;
            left: 80%;
            animation-delay: -3s;
            animation-duration: 20s;
            background: radial-gradient(circle, rgba(255,0,0,0.08) 0%, rgba(200,0,0,0.03) 70%);
        }

        .blood-cell:nth-child(3) {
            width: 100px;
            height: 100px;
            top: 80%;
            left: 20%;
            animation-delay: -5s;
            animation-duration: 25s;
            background: radial-gradient(circle, rgba(255,0,0,0.12) 0%, rgba(200,0,0,0.04) 70%);
        }

        .blood-cell:nth-child(4) {
            width: 180px;
            height: 180px;
            top: 30%;
            left: 50%;
            animation-delay: -7s;
            background: radial-gradient(circle, rgba(255,0,0,0.15) 0%, rgba(200,0,0,0.07) 70%);
        }

        .pulse {
            position: absolute;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle, rgba(255,0,0,0.03) 0%, rgba(255,0,0,0) 70%);
            animation: pulse 8s infinite ease-in-out;
            opacity: 0;
        }

        .heartbeat-line {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 100px;
            background: linear-gradient(90deg, transparent, rgba(200, 0, 0, 0.1), transparent);
            animation: heartbeat 1.5s infinite;
            transform-origin: center;
        }

        /* ===== KEYFRAME ANIMATIONS ===== */
        @keyframes float {
            0% { transform: translate(0, 0) rotate(0deg); }
            25% { transform: translate(50px, 50px) rotate(90deg); }
            50% { transform: translate(100px, -30px) rotate(180deg); }
            75% { transform: translate(-50px, 50px) rotate(270deg); }
            100% { transform: translate(0, 0) rotate(360deg); }
        }

        @keyframes pulse {
            0% { transform: scale(0.8); opacity: 0; }
            50% { opacity: 0.3; }
            100% { transform: scale(1.5); opacity: 0; }
        }

        @keyframes heartbeat {
            0% { transform: scaleX(0.8); opacity: 0.3; }
            14% { transform: scaleX(1.1); opacity: 0.5; }
            28% { transform: scaleX(0.9); opacity: 0.4; }
            42% { transform: scaleX(1.05); opacity: 0.5; }
            70% { transform: scaleX(1); opacity: 0.3; }
            100% { transform: scaleX(0.8); opacity: 0.3; }
        }

        /* ===== NAVIGATION BAR ===== */
        .navbar {
            background-color: rgba(255, 255, 255, 0.97);
            box-shadow: 0 2px 15px rgba(0, 0, 0, 0.1);
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 5%;
            backdrop-filter: blur(5px);
        }

        .logo {
            font-size: 1.8rem;
            font-weight: 700;
            color: #c00;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .logo::before {
            content: "🩸";
            font-size: 1.5rem;
        }

        .nav-links {
            display: flex;
            list-style: none;
            gap: 25px;
        }

        .nav-links a {
            text-decoration: none;
            color: #555;
            font-weight: 600;
            font-size: 1rem;
            transition: all 0.3s ease;
            position: relative;
            padding: 5px 0;
        }

        .nav-links a:hover {
            color: #c00;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 2px;
            background-color: #c00;
            transition: width 0.3s ease;
        }

        .nav-links a:hover::after,
        .nav-links a.active {
            width: 100%;
            color: #c00;
        }

        .btn {
            padding: 10px 25px;
            border-radius: 50px;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s ease;
            display: inline-block;
            cursor: pointer;
            text-align: center;
        }

        .btn-primary {
            background-color: #c00;
            color: white;
            border: 2px solid #c00;
        }

        .btn-primary:hover {
            background-color: #a00;
            border-color: #a00;
        }

        .btn-secondary {
            background-color: transparent;
            color: #c00;
            border: 2px solid #c00;
        }

        .btn-secondary:hover {
            background-color: #c00;
            color: white;
        }

        /* ===== PARTNER PAGE CONTENT ===== */
        .partner-content {
            margin-top: 80px;
            padding: 40px 5%;
            position: relative;
            z-index: 1;
            max-width: 1200px;
            margin-left: auto;
            margin-right: auto;
        }

        .hero-partner {
            text-align: center;
            padding: 60px 0 40px;
        }

        .hero-partner h1 {
            font-size: 2.8rem;
            color: #c00;
            margin-bottom: 20px;
            line-height: 1.3;
        }

        .hero-partner p {
            font-size: 1.2rem;
            color: #555;
            max-width: 800px;
            margin: 0 auto 30px;
            line-height: 1.6;
        }

        .partner-types {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin: 50px 0;
        }

        .partner-card {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
            transition: all 0.3s ease;
        }

        .partner-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(200, 0, 0, 0.1);
        }

        .partner-card h2 {
            color: #c00;
            margin-bottom: 15px;
            font-size: 1.8rem;
        }

        .partner-card ul {
            margin: 20px 0;
            padding-left: 20px;
        }

        .partner-card li {
            margin-bottom: 10px;
            color: #555;
            line-height: 1.6;
        }

        .partner-form-section {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 15px;
            padding: 40px;
            margin: 60px 0;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
        }

        .partner-form-section h2 {
            color: #c00;
            margin-bottom: 30px;
            font-size: 2rem;
            text-align: center;
        }

        .partner-form {
            max-width: 800px;
            margin: 0 auto;
        }

        .form-row {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 20px;
        }

        .form-group {
            flex: 1;
            min-width: 250px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #555;
            font-weight: 600;
        }

        .form-group input,
        .form-group select,
        .form-group textarea {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .form-group textarea {
            min-height: 120px;
            resize: vertical;
        }

        .form-group input:focus,
        .form-group select:focus,
        .form-group textarea:focus {
            border-color: #c00;
            outline: none;
            box-shadow: 0 0 0 3px rgba(200, 0, 0, 0.1);
        }

        .submit-btn {
            display: block;
            width: 200px;
            margin: 30px auto 0;
            padding: 15px;
            background-color: #c00;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .submit-btn:hover {
            background-color: #a00;
        }

        .benefits-section {
            margin: 60px 0;
        }

        .benefits-section h2 {
            color: #c00;
            margin-bottom: 40px;
            font-size: 2rem;
            text-align: center;
        }

        .benefits-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
        }

        .benefit-card {
            background: white;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
            transition: all 0.3s ease;
            text-align: center;
        }

        .benefit-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(200, 0, 0, 0.1);
        }

        .benefit-icon {
            font-size: 2.5rem;
            color: #c00;
            margin-bottom: 20px;
        }

        .benefit-card h3 {
            color: #333;
            margin-bottom: 15px;
        }

        .benefit-card p {
            color: #666;
            line-height: 1.6;
        }

        /* ===== RESPONSIVE DESIGN ===== */
        @media (max-width: 768px) {
            .navbar {
                padding: 15px;
            }
            
            .nav-links {
                position: fixed;
                top: 70px;
                left: -100%;
                width: 100%;
                height: calc(100vh - 70px);
                background-color: rgba(255, 255, 255, 0.98);
                flex-direction: column;
                align-items: center;
                padding-top: 40px;
                gap: 30px;
                transition: left 0.3s ease;
            }
            
            .nav-links.active {
                left: 0;
            }
            
            .partner-content {
                margin-top: 70px;
                padding: 20px;
            }
            
            .hero-partner h1 {
                font-size: 2.2rem;
            }
            
            .partner-card,
            .partner-form-section {
                padding: 25px;
            }
        }
    </style>
</head>
<body>
    <!-- Animated Background -->
    <div class="background">
        <div class="blood-cell"></div>
        <div class="blood-cell"></div>
        <div class="blood-cell"></div>
        <div class="blood-cell"></div>
        <div class="pulse"></div>
        <div class="heartbeat-line"></div>
    </div>

    <!-- Navigation Bar -->
    <nav class="navbar">
        <a href="/" class="logo">LifeLink</a>
        <ul class="nav-links">
            <li><a href="/">Home</a></li>
            <li><a href="/find-donor">Find Donor</a></li>
            <li><a href="/register">Register</a></li>
            <li><a href="/about">About</a></li>
            <li><a href="#" class="active">Partner</a></li>
        </ul>
        <div>
            <a href="/login" class="btn btn-secondary">Login</a>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="partner-content">
        <section class="hero-partner">
            <h1>Partner With LifeLink</h1>
            <p>Join forces with us to create a sustainable blood donation ecosystem. Together, we can ensure no patient dies waiting for blood.</p>
            <div class="action-buttons">
                <a href="#partner-form" class="btn btn-primary">Become a Partner</a>
                <a href="#benefits" class="btn btn-secondary">Learn More</a>
            </div>
        </section>

        <div class="partner-types">
            <div class="partner-card">
                <h2>For Hospitals</h2>
                <p>Benefits of partnering with LifeLink:</p>
                <ul>
                    <li>Access to verified blood donors 24/7</li>
                    <li>Real-time blood availability dashboard</li>
                    <li>Reduced blood shortage emergencies</li>
                    <li>Automated donor matching system</li>
                    <li>Community engagement programs</li>
                </ul>
            </div>
            <div class="partner-card">
                <h2>For Blood Banks</h2>
                <p>How we collaborate with blood banks:</p>
                <ul>
                    <li>Centralized donor database</li>
                    <li>Campaign management tools</li>
                    <li>Donor retention programs</li>
                    <li>Inventory management integration</li>
                    <li>Data analytics and reporting</li>
                </ul>
            </div>
            <div class="partner-card">
                <h2>For Organizations</h2>
                <p>Corporate and community partnerships:</p>
                <ul>
                    <li>Employee blood donation drives</li>
                    <li>CSR initiative support</li>
                    <li>Sponsorship opportunities</li>
                    <li>Volunteer programs</li>
                    <li>Public awareness campaigns</li>
                </ul>
            </div>
        </div>

        <section id="benefits" class="benefits-section">
            <h2>Partnership Benefits</h2>
            <div class="benefits-grid">
                <div class="benefit-card">
                    <div class="benefit-icon">📈</div>
                    <h3>Increased Supply</h3>
                    <p>Access to our network of 25,000+ registered donors to meet your blood requirements</p>
                </div>
                <div class="benefit-card">
                    <div class="benefit-icon">⏱️</div>
                    <h3>Faster Response</h3>
                    <p>Our automated matching system connects you with donors in minutes during emergencies</p>
                </div>
                <div class="benefit-card">
                    <div class="benefit-icon">🔄</div>
                    <h3>Regular Donors</h3>
                    <p>Our reminder system ensures donors return every 3 months for repeat donations</p>
                </div>
                <div class="benefit-card">
                    <div class="benefit-icon">📊</div>
                    <h3>Data Insights</h3>
                    <p>Receive detailed analytics about donation patterns and blood type demand</p>
                </div>
            </div>
        </section>

        <section id="partner-form" class="partner-form-section">
            <h2>Partner Inquiry Form</h2>
            <form class="partner-form">
                <div class="form-row">
                    <div class="form-group">
                        <label for="organization">Organization Name*</label>
                        <input type="text" id="organization" required>
                    </div>
                    <div class="form-group">
                        <label for="type">Organization Type*</label>
                        <select id="type" required>
                            <option value="">Select Type</option>
                            <option value="hospital">Hospital/Clinic</option>
                            <option value="blood-bank">Blood Bank</option>
                            <option value="corporate">Corporate</option>
                            <option value="ngo">NGO/Non-Profit</option>
                            <option value="other">Other</option>
                        </select>
                    </div>
                </div>
                <div class="form-row">
                    <div class="form-group">
                        <label for="contact-name">Contact Person*</label>
                        <input type="text" id="contact-name" required>
                    </div>
                    <div class="form-group">
                        <label for="designation">Designation*</label>
                        <input type="text" id="designation" required>
                    </div>
                </div>
                <div class="form-row">
                    <div class="form-group">
                        <label for="email">Email*</label>
                        <input type="email" id="email" required>
                    </div>
                    <div class="form-group">
                        <label for="phone">Phone*</label>
                        <input type="tel" id="phone" required>
                    </div>
                </div>
                <div class="form-group">
                    <label for="address">Address</label>
                    <input type="text" id="address">
                </div>
                <div class="form-group">
                    <label for="message">How would you like to partner with us?*</label>
                    <textarea id="message" required></textarea>
                </div>
                <button type="submit" class="submit-btn">Submit Application</button>
            </form>
        </section>
    </main>

    <script>
        // Mobile menu toggle
        document.addEventListener('DOMContentLoaded', function() {
            const hamburger = document.createElement('div');
            hamburger.className = 'hamburger';
            hamburger.innerHTML = '<span></span><span></span><span></span>';
            document.querySelector('.navbar').appendChild(hamburger);
            
            hamburger.addEventListener('click', function() {
                document.querySelector('.nav-links').classList.toggle('active');
                hamburger.classList.toggle('active');
            });

            // Form submission
            const partnerForm = document.querySelector('.partner-form');
            partnerForm.addEventListener('submit', function(e) {
                e.preventDefault();
                alert('Thank you for your partnership interest! We will contact you shortly.');
                partnerForm.reset();
            });
        });
    </script>
</body>
</html>